#pragma once

// Selectable languages to use
enum class Language {
    English,
    Russian,

    // Counter of all languages
    Count,

    // Standart pre-selected language
    Default = English,
};
